"use client";
import { RouteStub } from "../_stub";

export default function ResumeStub() {
  return (
    <RouteStub
      eyebrow="Resume · agent"
      title="Tailor your resume"
      italic="to one job."
      sub="The on-file + tailor + tailored-history layout (with ATS scoring) lands in PR 4."
      hindi="रेज़्यूमे"
      legacyHref="/resume"
      legacyLabel="Open legacy Resume"
    />
  );
}
