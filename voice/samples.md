# Voice Samples

Paste 3-5 of your real outreach emails below as fenced code blocks. Each one becomes a few-shot example for the `draft()` function.

Format:

```
[paste full email body — short, the way you actually write]
```

```
[next sample]
```

Notes:
- Keep them short and punchy.
- Mix channels (email, LinkedIn, X DM) if you want channel-specific voice — label them with a leading `# email`, `# linkedin`, `# x_dm` line inside the fence.
- The Voice tuning loop (Prompt 9) appends edited drafts here automatically.
