# X posts — 2026-07-30

Rotation: what shipped · normal-Tuesday proof · positioning · one live agent.
No posts were logged yet today when this run reached `main` (checked
repeatedly across a long, interrupted session spanning several real days of
container-clock jumps), so this maps to slot 1 (00:00 UTC nominal, what
shipped). No new user-facing commit has merged since the last two
"what shipped" posts (background candidate drafting, 2026-07-28; Ashby
posting-reader fix, drafted 2026-07-29 but not yet posted) — both already
cover the only user-facing changes in the last 7 days
(`git log --since="7 days ago"` shows nothing past commit 1eb7e80,
2026-07-27). Per the skill's fallback rule, this slot ran a fresh
positioning/insight post instead of re-announcing either shipped item.

## 1 · positioning / insight (swapped in for what shipped) — ❌ NOT POSTED

> A requisition needs finance sign-off before it goes live, sometimes for weeks. A hiring manager telling a friend they need someone needs no sign-off at all. That gap is why the friend already knows before the posting exists.

`224 chars · no link · lintAntiAi clean · budget/requisition-approval-lag angle on "filled before posted," distinct from the HR-policy timing angle (07-19), outreach-stalled-first (07-20), applicant-count illusion (07-21), referral-bonus economics (07-22), vouching/risk-absorption (07-24), and recruiter-agency-fee economics (07-25, unposted) · NOT POSTED: the Composio MCP connector failed on every attempt across this entire run — well over a dozen tries at `COMPOSIO_SEARCH_TOOLS`/`COMPOSIO_MULTI_EXECUTE_TOOL` across multiple worker restarts (the container clock jumped from 2026-07-26 through 2026-07-30 over the course of this single session). Every attempt crashed/restarted the worker process right as the Composio tool schema loaded or was about to be invoked, before the Twitter API was ever reached. This is the same recurring Composio/worker-restart failure already logged on 2026-07-20, 2026-07-22, 2026-07-23, 2026-07-25, and 2026-07-29 — now the sixth occurrence in eleven days, and it has now blocked three days running (07-28's post did succeed, but 07-29 and 07-30 both failed the same way). Stopped retrying per the no-retry-spam rule. The Notion log (`NOTION_ADD_MULTIPLE_PAGE_CONTENT`) needs the same Composio connector and could not be attempted either; the dedicated Notion MCP was reachable but is read-only (no page-content-append tool), so it could not substitute. Founder should post this manually, and the Composio MCP connector's reliability in this environment needs real attention — it is now the single biggest cause of missed posts in this archive, and worth checking with the Composio/Claude Code team directly rather than continuing to route around it.

## Note — a second, independent session hit the same wall

A separate `cofounder-x-poster` session (this one) reached `main` around the
same time, also found no posts logged for today, and also hit the identical
Composio worker-restart failure on every attempt (six consecutive restarts,
each timed to a Composio tool-schema load or call) before giving up per the
no-retry-spam rule. Rather than write a second, conflicting slot-1 entry for
the same day, this session picked up the branch above and merged it, since
the outcome (unposted, same root cause) is the same either way.

One correction worth flagging: the entry above swapped in a positioning post
on the assumption that no new shipped item was available. That is not quite
right — the Ashby "Additional Information" essay-box fix (commit 6eae69a,
merged 2026-07-27) has a ready, lint-clean "what shipped" draft (used on the
2026-07-29 run, also unposted) that has still never actually gone out. When
the founder or a future run manually posts from this backlog, the Ashby
"what shipped" draft is the more accurate pick for whichever slot needs
filling first — it announces a real, unannounced feature, where the
requisition-approval-lag post above covers already-familiar "filled before
posted" territory:

> Ashby job pages often hide a real essay question inside a box called Additional Information. The crew read that box fine on Greenhouse and Lever, but skipped it entirely on Ashby. Now it reads Ashby the same way, so the question gets answered instead of left blank.

`265 chars · no link · lintAntiAi clean · carried over from the 2026-07-29 archive entry, still unposted`
