# X posts — 2026-08-03

Rotation: what shipped · normal-Tuesday proof · positioning · one live agent.
No posts were logged yet today when this run reached `main`, so this maps to
slot 1 (00:00 UTC nominal, what shipped). Two new user-facing commits landed
since the last "what shipped" post (background candidate drafting,
2026-07-28): commit 78f80d8 stopped a typed request like "find me people at
Macy's who are hiring" from being misrouted into the broken Apply/job flow,
and commit 3905d86 (built on top of it) made that category-style "find
people at a company" ask return an actual shortlist of candidates instead of
dead-ending with "couldn't identify a specific person." Picked the combined
story over either half alone or the stale Ashby draft (still unposted since
2026-07-29, five straight days now) because it is a complete, concrete,
user-facing win end to end, and neither half has been posted before.

## 1 · what shipped — NOT ATTEMPTED (infrastructure outage, see note)

> Typed find people at Macy's in supply chain, no name, no link, just a category. That used to dead end with couldn't identify a specific person. Now it comes back with a real shortlist of people there. Pick one and the run keeps going.

`234 chars · no link · lintAntiAi clean · what-shipped angle combining the Apply-misroute fix (commit 78f80d8) and the free-text shortlist fallback (commit 3905d86), both merged 2026-08-02/03 and not previously covered`

## Note — Composio MCP connector unreachable this entire run; escalating

This run spanned container-clock jumps from roughly 2026-07-31 11:00 UTC
through 2026-08-03 10:43 UTC, across at least eight separate worker-process
restarts. Every restart happened at the same point: the instant a Composio
tool (`ToolSearch` resolving `mcp__Composio__COMPOSIO_SEARCH_TOOLS` /
`COMPOSIO_MULTI_EXECUTE_TOOL` / `COMPOSIO_MANAGE_CONNECTIONS`, or a call into
one of them) loaded or was about to be invoked — before the Twitter API, or
Notion, was ever reached. One restart also hit a plain local `git fetch`/
`git show` call in the same window, so the instability is not perfectly
scoped to Composio, though every Composio attempt specifically failed.
`TWITTER_CREATION_OF_A_POST` and `NOTION_ADD_MULTIPLE_PAGE_CONTENT` were
never actually attempted (the tool call itself never completed setup, not
"tried and got an error"). Per the no-retry-spam rule, this run stopped
after eight restarts rather than continuing indefinitely.

This is the same recurring Composio/worker-restart failure logged on
2026-07-20, 07-22, 07-23, 07-25, 07-29, 07-30, twice on 07-31, and on
08-02 (which also reported an entire missed day, 2026-08-01, with zero
posts logged at all). This is now the eleventh occurrence in roughly two
weeks and the account has not successfully posted since 2026-07-28 — a
five-day-and-counting outage as of this entry. Founder should post the
draft above manually, and the Composio MCP connector's reliability in this
environment needs direct attention: it is no longer an occasional flake,
it is the single biggest blocker to this skill functioning at all.
