# X posts — 2026-08-09

Rotation: what shipped · normal-Tuesday proof · positioning · one live agent.

Note on dating: this session's container clock reported 2026-08-02 at the
start of this run (`date -u` said 2026-08-02 17:09 UTC), but the CLAUDE.md
system context gives the authoritative current date as 2026-08-09, and
`origin/main` was already 8 archive-days and dozens of commits ahead of
that stale local clock (entries exist through 2026-08-07). This is the
same container-clock-drift issue already documented repeatedly in this
archive (07-25 onward). Filed under the real date. No entry existed yet
for 2026-08-09 when this run reached `main`, so this maps to slot 1
(00:00 UTC nominal, what shipped).

No new user-facing commit has merged since the free-text people-shortlist
fallback (commit 3905d86) and the Apply-misrouting fix (commit 78f80d8),
both already covered by the 2026-08-03/08-05 "what shipped" drafts
(still unposted) — `git log origin/main --since="14 days ago"` shows
nothing past those two feature commits. Per the skill's fallback rule,
this run swapped in a fresh positioning/insight post rather than
re-announcing an already-drafted shipped item.

## 1 · positioning / insight (swapped in for what shipped) — ❌ NOT POSTED

> A LinkedIn Easy Apply click takes ten seconds and gets read for less than that. A message straight to the hiring manager takes ten minutes to get right and gets read every time. Effort and attention run in opposite directions.

`226 chars · no link · lintAntiAi clean (checked by hand against the FORBIDDEN_PHRASES/FORBIDDEN_WORDS/em-dash/arrow/negative-parallelism/participle-ending rules in src/lib/writing/anti-ai.ts) · effort-vs-attention angle, distinct from every prior positioning post in this archive: HR-policy-timing (07-19), backup-plan-after-quiet-way-stalls (07-20), applicant-count illusion (07-21), referral-bonus economics (07-22), vouching/risk-absorption (07-24), recruiter-agency-fee economics (07-25, unposted), requisition-approval-lag (07-30), aggregator-lag (08-02), and JD-as-paperwork (08-04)`

**NOT POSTED — confirmed session-level permission gate, not Composio flakiness.**
An earlier attempt this same run (a different draft, an Outreach Bhai
proof story) called `TWITTER_CREATION_OF_A_POST` via
`mcp__Composio__COMPOSIO_MULTI_EXECUTE_TOOL` and got back an explicit
denial: "Permission for this action was denied by the Claude Code auto
mode classifier. Reason: Blocked by classifier." This matches the same
gate already independently confirmed on 2026-08-03 ("the denial is a
session-level gate on irreversible public actions for unattended runs,
not content-dependent") and earlier on 2026-07-20 and 2026-07-23. Per
that prior finding and the no-retry-spam rule, this run did not attempt
the call a second time with this slot's own draft — the outcome would be
the same regardless of content.

Separately, this run also hit the ordinary Composio worker-restart
instability (documented on nearly every prior archive day since 07-20)
repeatedly while trying to reach `NOTION_ADD_MULTIPLE_PAGE_CONTENT`; the
Notion log could not be completed and this repo archive entry is the
durable record instead.

**Status: the @OpenStreetExch/@JugaaduApp account has not successfully
posted via this skill since 2026-07-28 — now 12+ calendar days across at
least twenty documented failed attempts, split between two distinct,
independently-confirmed root causes: (1) the Composio MCP connector's
worker-restart instability, and (2) an auto-mode permission classifier
that appears to categorically block unattended, scheduled sessions from
completing a direct-to-X publish action, regardless of draft content. The
first is intermittent infrastructure flakiness. The second looks
structural: if scheduled/unattended runs of this skill can never pass the
classifier for an irreversible public-posting action, no amount of retry
or draft-quality improvement will fix it. Founder should either (a) adjust
the Claude Code auto-mode / permission settings for this environment to
allow Composio's `TWITTER_CREATION_OF_A_POST` for this scheduled routine,
or (b) change this skill's contract from auto-publish to draft-and-approve
so a human confirms the final post. A backlog of ready, lint-clean,
unposted drafts has accumulated across 07-29 through 08-09 in this
archive for manual posting in the meantime.**
