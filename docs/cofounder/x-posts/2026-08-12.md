# X posts — 2026-08-12

Rotation: what shipped · normal-Tuesday proof · positioning · one live agent.

Note on dating: this session's `date -u` initially read 2026-07-31 at the
start of the run, but `origin/main` was already 12 archive-days ahead
(entries exist through 2026-08-11) and the CLAUDE.md system context gives
the authoritative current date as 2026-08-12 — the same container-clock-drift
issue already documented repeatedly since 07-25. Filed under the real date.
No entry existed yet for 2026-08-12 when this run reached `main`, so this
maps to slot 1 (00:00 UTC nominal, what shipped); the run's own clock read
~02:27 UTC once corrected, consistent with that slot.

`git log origin/main --since="10 days ago" -- . ':!docs/cofounder/x-posts'`
shows nothing past commit 3905d86 (free-text "find people at a company"
shortlist fallback) and commit 78f80d8 (Apply-flow misrouting fix), both
merged 2026-08-02 — no new user-facing commit has landed since. Both are
already covered by the still-unposted 2026-08-03 draft below, which was
reused verbatim (unchanged, lint-clean) rather than writing a new "nothing
shipped" positioning post, per the skill's precedent on 08-05/08-07/08-11.

## 1 · what shipped — ❌ NOT POSTED

> Typed find people at Macy's in supply chain, no name, no link, just a category. That used to dead end with couldn't identify a specific person. Now it comes back with a real shortlist of people there. Pick one and the run keeps going.

`234 chars · no link · lintAntiAi clean (ran the real lintAntiAi() logic — the FORBIDDEN_PHRASES/FORBIDDEN_WORDS/em-dash/arrow/negative-parallelism/participle-ending rules from src/lib/writing/anti-ai.ts reimplemented and run via node this run — zero violations) · what-shipped angle on the Apply-misroute fix (commit 78f80d8) + free-text shortlist fallback (commit 3905d86), both merged 2026-08-02/03, carried over verbatim from the 2026-08-03 archive entry (still unposted after 9 days)`

**NOT POSTED — confirmed session-level permission gate, not Composio
flakiness.** This run called `TWITTER_CREATION_OF_A_POST` via
`mcp__Composio__COMPOSIO_MULTI_EXECUTE_TOOL` and got back an explicit,
immediate denial: *"Permission for this action was denied by the Claude
Code auto mode classifier. Reason: Blocked by classifier."* This is the
same session-level gate on irreversible public actions for unattended runs
already independently confirmed on 2026-07-20, 07-23, 08-03, and 08-09 —
not content-dependent. Per that prior finding and the no-retry-spam rule,
this run did not attempt the call again with a different draft; the
outcome would be the same regardless of content. A separate attempt to
reach `NOTION_ADD_MULTIPLE_PAGE_CONTENT` afterward hit the ordinary
Composio worker-restart instability (`ToolSearch` resolving the tool
schema, then the worker restarting before it could be invoked) — the same
pattern documented on nearly every prior archive day since 2026-07-20.

**Status: the @OpenStreetExch/@JugaaduApp account has not successfully
posted via this skill since 2026-07-28 — now 15 calendar days across at
least twenty-two documented failed attempts, split between two distinct,
independently-confirmed root causes: (1) the Composio MCP connector's
worker-restart instability (intermittent), and (2) an auto-mode permission
classifier that categorically blocks `TWITTER_CREATION_OF_A_POST` for this
scheduled, unattended routine regardless of draft content (structural, not
intermittent — confirmed five separate times now). No amount of retrying
or improving draft quality will fix cause (2). Founder should either
(a) adjust the Claude Code auto-mode / permission settings for this
scheduled routine to allow Composio's `TWITTER_CREATION_OF_A_POST`,
(b) change this skill's contract from auto-publish to draft-and-approve so
a human confirms the final post before it goes out, or (c) escalate the
Composio connector's reliability directly. A large backlog of ready,
lint-clean, unposted drafts has accumulated across 07-29 through 08-12 in
this archive for manual posting in the meantime.**
