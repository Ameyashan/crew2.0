# X posts — 2026-07-25

Rotation: what shipped · normal-Tuesday proof · positioning · one live agent.
No posts were logged yet today when this run reached `main` (checked
repeatedly across a long, interrupted session — last check at 23:32 UTC), so
this maps to slot 1 (00:00 UTC nominal, what shipped). Nothing user-facing
has merged to main since the follow-companies feature (2026-07-21) — the
same gap already covered by the 07-22/07-24 "nothing shipped" fallback posts
— so this slot ran a fresh positioning/insight angle instead of re-announcing
that feature or inventing a changelog line, per the skill's fallback rule.

## 1 · positioning / insight (swapped in for what shipped) — ❌ NOT POSTED

> An external recruiter usually costs a company fifteen to twenty five percent of a role's first year salary. That is why a hiring manager spends two weeks texting people they trust before a requisition goes live. The board is what is left once the free options run out.

`268 chars · no link · lintAntiAi clean · recruiter-agency-fee economics angle on "filled before posted," distinct from HR-policy timing (07-19), outreach-stalled-first (07-20), applicant-count illusion (07-21), referral-bonus economics (07-22), and vouching/risk-absorption (07-24) · NOT POSTED: the Composio MCP connector failed on every attempt this run (7 consecutive tries across `COMPOSIO_SEARCH_TOOLS` and `COMPOSIO_GET_TOOL_SCHEMAS`, spanning multiple MCP reconnect cycles) — each call crashed/restarted the session's worker process before it could reach the Twitter API, regardless of query payload or which Composio meta-tool was called first. This matches the same recurring Composio/worker-restart failure already logged on 2026-07-20, 2026-07-22, and 2026-07-23. Retries were stopped after a handful of attempts (not spammed further) specifically to avoid the multi-hour session stall that caused duplicate posts on 2026-07-24 — this session's own container clock jumped from 2026-07-23 to 2026-07-25 mid-run across these same restarts. The Notion log (`NOTION_ADD_MULTIPLE_PAGE_CONTENT`) needs the same Composio connector and could not be attempted either. Founder should post this manually, and the Composio MCP server's stability in this environment is worth a look — this is now the fourth run in ~5 days to hit it.
