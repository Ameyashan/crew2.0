# X posts — 2026-08-11

Rotation: what shipped · normal-Tuesday proof · positioning · one live agent.

No entry existed yet for 2026-08-11 when this run reached `main` (last
dated entry was 2026-08-09, positioning). This run's own container clock
was unstable across the session — repeated worker restarts moved it from
an apparent 2026-08-02/07/09 forward to a confirmed-real 2026-08-11
18:13 UTC (cross-checked against `origin/main` and the session's system
date context each time) — so this maps to the 18:00 UTC nominal slot
(one live agent, in depth). Rotation is Resume Darzi -> Person Khoji ->
Email Wallah -> Outreach Bhai; the last true "one live agent" spotlight
was Email Wallah (2026-08-07), so this run moves to Outreach Bhai (last
spotlighted 2026-07-24, followup-queue mechanism angle). Wrote a fresh
angle not used by any prior Outreach Bhai post: it does not send the same
line on every channel, the register changes per channel while the hook
stays the same. Distinct from the hook/voice framing (07-18) and the
followup-queue mechanism (07-24, posted) and the hook-finding proof story
(08-04, unposted, proof-slot not spotlight-slot).

No new user-facing commit has merged since the free-text people-shortlist
fallback (commit 3905d86) and the Apply-misrouting fix (commit 78f80d8),
both merged 2026-08-02 and already covered by unposted 08-03/08-05
drafts — `git log origin/main --since="10 days ago" -- . ':!docs/cofounder/x-posts'`
confirms nothing past those two.

## 4 · Outreach Bhai (live agent) — ❌ NOT POSTED

> Outreach Bhai does not send the same line three times. The email reads formal, the LinkedIn note is shorter, the X DM drops straight into the point. Same hook, three different registers, because nobody writes to a hiring manager on X the way they would email her.

`263 chars · no link · lintAntiAi clean (checked by hand against the FORBIDDEN_PHRASES/FORBIDDEN_WORDS/em-dash/arrow/negative-parallelism/participle-ending rules in src/lib/writing/anti-ai.ts; no tsx/ts-node available in this session to run the module directly) · Outreach Bhai in-depth spotlight, per-channel-register angle, distinct from every prior Outreach Bhai post listed above`

**NOT POSTED.** This run reproduced the same Composio MCP connector
failure documented on every archive day since 2026-07-20, and explicitly
diagnosed as a likely structural block on the 2026-08-09 entry (an
earlier attempt that run got an explicit `TWITTER_CREATION_OF_A_POST`
denial: "Permission for this action was denied by the Claude Code auto
mode classifier. Reason: Blocked by classifier."). This run's own
failures never got that far: repeated `ToolSearch` calls resolving
`mcp__Composio__COMPOSIO_MULTI_EXECUTE_TOOL` restarted the session's
worker process immediately after the tool schema loaded, well over
fifteen times across this single session, before `TWITTER_CREATION_OF_A_POST`
could ever be invoked. `NOTION_ADD_MULTIPLE_PAGE_CONTENT` needs the same
connector and could not be attempted for the same reason.

**Status: the @OpenStreetExch/@JugaaduApp account has not successfully
posted via this skill since 2026-07-28 — now 14 calendar days across at
least twenty-one documented failed attempts.** Two distinct,
independently-confirmed root causes are active: (1) the Composio MCP
connector's worker-restart instability (this run's failure mode), and
(2) an auto-mode permission classifier that categorically blocked the
one 2026-08-09 attempt that actually reached the API call, regardless of
draft content. Founder should either (a) adjust the Claude Code auto-mode
/ permission settings for this scheduled routine to allow Composio's
`TWITTER_CREATION_OF_A_POST`, or (b) change this skill's contract from
auto-publish to draft-and-approve so a human confirms the final post
before it goes out, or (c) investigate the Composio connector's
reliability directly with the Claude Code / Composio team. A large
backlog of ready, lint-clean, unposted drafts has accumulated across
07-29 through 08-11 in this archive for manual posting in the meantime.
