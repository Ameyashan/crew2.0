# X posts — 2026-08-02

Rotation: what shipped · normal-Tuesday proof · positioning · one live agent.
No posts were logged for 2026-08-01 at all (the whole day was missed), so
the day's first run mapped to slot 1 (00:00 UTC nominal, what shipped). Two
more independent `cofounder-x-poster` runs picked up later the same day and
logged slots 2 and 3 below. No new user-facing commit has merged since the
Ashby posting-reader fix (commit 6eae69a, 2026-07-27) and the background
candidate-drafting change (commit 1eb7e80, 2026-07-27, posted 2026-07-28) —
`git log --since="7 days ago"` still shows nothing past those two — so every
"what shipped" slot today either reused the still-unposted Ashby draft or
swapped in a positioning/agent post per the skill's fallback rule.

## 1 · what shipped — NOT ATTEMPTED (infrastructure outage, see note)

> Ashby job pages often hide a real essay question inside a box called Additional Information. The crew read that box fine on Greenhouse and Lever, but skipped it entirely on Ashby. Now it reads Ashby the same way, so the question gets answered instead of left blank.

`265 chars · no link · lintAntiAi clean · what-shipped angle on the Ashby posting-reader fix (commit 6eae69a, merged 2026-07-27) · reused verbatim, still unposted after five straight days of attempts: 07-29, 07-30, 07-31 x2, 08-02`

## 2 · positioning / insight — NOT POSTED (infrastructure outage, see note)

A separate `cofounder-x-poster` run picking up later. Slot 1 (above) was
already logged and unposted for today, so this run mapped to the next
nominal slot reached during its own span (12:00 UTC, positioning/insight).

> By the time a role shows up on LinkedIn or Indeed, it passed through the company careers page, then an ATS sync, then a job board scrape. That lag runs days, sometimes a week. Message the hiring manager the day it opens on the company site, not the day an aggregator finds it.

`276 chars · no link · lintAntiAi clean · aggregator-lag angle on "filled before posted," distinct from HR-policy timing (07-19), applicant-count illusion (07-21), referral-bonus economics (07-22), vouching/risk-absorption (07-24), recruiter-agency-fee economics (07-25, unposted), and finance-signoff-gap (07-30, unposted) · NOT POSTED: this run's own session hit the identical Composio/worker-restart wall documented below. One `COMPOSIO_SEARCH_TOOLS` call did complete mid-run and confirmed the `twitter` connection is ACTIVE on @JugaaduApp (account id 2018174211513110528), but every subsequent attempt at the actual `TWITTER_CREATION_OF_A_POST` call restarted the worker instead of completing.`

## 3 · normal Tuesday proof — NOT POSTED (infrastructure outage, see note)

A third, separate `cofounder-x-poster` run (fired ~05:09 UTC). Two posts
were already logged for 2026-08-02 (slots 1 and 2 above, both unposted)
when this run reached `main`, so per the counting rule this maps to the
day's third logged entry — normal-Tuesday proof in the rotation. No new
user-facing commit has merged since the Ashby fix (verified independently:
`git log origin/main --since="7 days ago"` shows nothing past
1eb7e80/6eae69a, 2026-07-27). Person Khoji was last featured as a proof
story on 2026-07-26 (dead-end careers-page form / decision-maker-not-inbox
angle) and as the "one live agent" spotlight on 2026-07-20 (ranking a
twelve-person team page); this draft uses a different angle — a stale
team-page listing catching an employee who already left — grounded in the
"cross-checks LinkedIn" mechanic from the live agent's own teaser copy
(`src/app/page.tsx`, `AGENTS_V3`).

> The team page still listed their old VP of Engineering as the contact. He left three months back. Person Khoji cross-checked LinkedIn, caught it, and pointed the message at whoever runs the req now instead of an inbox nobody checks anymore.

`240 chars · no link · lintAntiAi clean (checked by hand against the FORBIDDEN_PHRASES/FORBIDDEN_WORDS/em-dash/arrow/negative-parallelism/participle-ending rules in src/lib/writing/anti-ai.ts, since the connector that would run the real linter/poster was unreachable) · Person Khoji proof story, stale-team-page/departed-employee angle, distinct from the 07-26 dead-end-form proof and the 07-20 twelve-person-ranking spotlight · NOT POSTED: see note below`

## Note — Composio MCP connector down all day, now the biggest blocker to this skill

Every run today (and most runs across the prior two weeks) hit the same
failure: a Composio tool call — `COMPOSIO_SEARCH_TOOLS`,
`COMPOSIO_GET_TOOL_SCHEMAS`, or `COMPOSIO_MULTI_EXECUTE_TOOL` — restarts the
session's worker process right as the tool is resolved or invoked, before
`TWITTER_CREATION_OF_A_POST` or `NOTION_ADD_MULTIPLE_PAGE_CONTENT` is ever
reached. This run alone hit five such restarts in a row (one `SEARCH_TOOLS`
call did briefly succeed and confirmed the `twitter` connection is ACTIVE
on @JugaaduApp, account id 2018174211513110528 — so the connection itself
is fine, only the follow-through call is crashing). Stopped retrying per
the no-retry-spam rule rather than continuing indefinitely.

This is the same recurring failure already logged on 2026-07-20, 07-22,
07-23, 07-25, 07-29, 07-30, twice on 07-31, and now three times on 08-02 —
at least twelve occurrences in two weeks. Combined with the fully missed
day on 2026-08-01, this is no longer an intermittent flake: it is currently
blocking nearly every run of this skill outright. The backlog of ready,
lint-clean, unposted drafts (Ashby "what shipped," the aggregator-lag
positioning post, and the Person Khoji proof story above) keeps growing.
Founder should post any of the three manually if desired, and the Composio
MCP connector's reliability in this environment needs direct attention —
this is now the top blocker to the skill functioning at all.
