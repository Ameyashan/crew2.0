# Security — 2026-07-10 (first live standup)

Nothing shipped today (all findings are proposals — each changes prod behavior
and needs your call). No new/changed API routes since the foundation landed.

## New finding: the anonymous rate limit is a no-op in production
`anon_run_events` (migration `0013`) **was never applied to the production
database** (confirmed via Supabase — the table doesn't exist). Consequences:
- `src/lib/anon-rate-limit.ts` fails **open** on the missing table (catches the
  error, returns "allow"), so **signed-out visitors are NOT capped at one free
  run** — the blur-gate teaser can be run unlimited times per IP. That's
  uncapped, unauthenticated LLM + web-search compute — a cost/abuse exposure.
- Fix is to apply `0013` (creates the table). But doing so **activates** the
  1-run/day cap that's currently dormant — a real product behavior change — so
  it's your call, not an auto-ship. Recommend applying it (the cap is the intended
  behavior) and confirming the blur-gate still feels right.

## Standing follow-ups (unchanged, need your inputs)
1. **Fail-close the cron/webhook guards** — confirm `CRON_SECRET` and
   `INBOUND_WEBHOOK_TOKEN` are set in Vercel prod first, else this 401s the crons.
2. **Enforce CSP** — currently Report-Only. After one clean deploy of the report
   stream, flip to enforcing. (No reports reviewed yet — needs a deploy first.)
3. **Auth + rate-limit the resume export routes** (`/api/resume/pdf`, `/docx`) —
   confirm there's no signed-out export path, then gate them.
4. `ADMIN_ANALYTICS_SECRET` — set ✅ (you did this today). Admin analytics API is live.

## Verified healthy
- RLS is enabled on every production table (spot-checked via Supabase).
- No secrets committed; `.env*` gitignored.
- Security headers (HSTS/X-Frame-Options/nosniff/Referrer-Policy/Permissions-Policy)
  shipped; CSP in Report-Only.
